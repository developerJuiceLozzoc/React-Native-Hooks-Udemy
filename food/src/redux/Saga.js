/*
function take("request")
if an action is dispatched with action of "requst", it iwll continue

call(fetch,req.payload), call will invoke a function with a payload
put(type,payload) once the call promise is resolved, we can call this put function


 race will occur multiple processes and cancel the loser

*/
